---
title: Docker
weight: 12
---

# Docker

Docker is used to package the Connect codebase up and provide version builds.

Development workflow consists of making changes then pushing the code which is then rebuilt into a container via [Concourse](/components/concourse). This is then deployed to [EKS](/components/eks).